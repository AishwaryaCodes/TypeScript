// Implement a FIFO queue API in TypeScript that supports multiple producers/consumers.
// Required: enqueue(item): Promise<void>, 
// dequeue(): Promise<T>, 
// size getter, and a capacity (block/await when full or empty).
// If time: tryEnqueue/tryDequeue, close(), timeouts. No libs.
 